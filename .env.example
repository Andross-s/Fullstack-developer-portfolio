# Base URL of the deployed frontend (used for metadata/OG tags)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Base URL of the Express contact-form API (see /server)
NEXT_PUBLIC_API_URL=http://localhost:4000
