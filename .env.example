# Base URL of the deployed frontend (used for metadata/OG tags)
# Local dev: leave unset, code falls back to http://localhost:3000
NEXT_PUBLIC_SITE_URL=https://fullstack-developer-portfolio-eta.vercel.app

# Base URL of the Express contact-form API (see /server)
# Local dev: leave unset, code falls back to http://localhost:4000
NEXT_PUBLIC_API_URL=https://fullstack-developer-portfolio-4l5j.onrender.com
